
```javascript

```

rust
```rust
    // Array dari array byte yang mewakili karakter Jepang
    let bytes_arrays: Vec<&[u8]> = vec![
        &[0xe3, 0x81, 0x94], // ご (go)
        &[0xe3, 0x81, 0xaf], // は (ha)
        &[0xe3, 0x81, 0xbf], // み (mi)
    ];
```
